## 1. 后端

- [x] 1.1 新增注册 DTO、Service、公开接口和 JWT 放行路径
- [x] 1.2 接入验证码、BCrypt、唯一约束冲突处理、限流与防重，中英错误文案同步
- [x] 1.3 测试注册成功、重复账号、验证码失败/重放、参数校验、权限字段隔离及匿名访问边界

## 2. 前端

- [x] 2.1 接入注册 API、注册表单、验证码弹窗和失败重试
- [x] 2.2 成功返回登录、一次性回填用户名、保持密码为空，补齐中英双语
- [x] 2.3 同步 README 项目亮点、功能清单和注意事项

## 3. 验证与归档

- [x] 3.1 运行 mvn -q compile、mvn test、pnpm check:type 并验证浏览器注册导航与输入校验
- [x] 3.2 执行 openspec validate add-captcha-registration --strict
- [x] 3.3 归档 openspec 变更（sync delta → archive）

## 验证记录

- 后端：Java 21 下 `mvn -q compile` 通过；`mvn -q -Dmaven.compiler.proc=full compile test` 共 99 项测试通过（含 30 项新增注册测试；Java 25 需显式启用 Lombok 注解处理）。
- 前端：`pnpm check:type` 通过。
- 真实服务：通过前端 5777 代理至后端 18080，验证注册成功且不签发凭证、验证码重放与重复账号被拒绝、新账号登录、个人中心首页、空角色与权限码、管理接口返回 403。临时账号与登录日志已删除，会话已退出，用户数恢复为 4。
- 浏览器：必填、用户名/密码格式、两次密码一致校验，验证码加载与主动刷新、取消弹窗，注册/登录多次往返、返回后密码清空，中英文页面切换均通过。未在浏览器中代填验证码；成功注册和登录由真实 API 联调验证。
