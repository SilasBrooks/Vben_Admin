## Tasks

- [x] 调整安全邮箱页面为同屏表单、验证码右侧发送按钮和独立绑定按钮
- [x] 隔离发送/绑定加载状态，处理冷却、重发、修改输入和失败重试
- [x] 为绑定与找回的发码 API 设置独立超时
- [x] 同步中英双语文案与 README
- [x] 完成类型检查、针对性回归验证及 OpenSpec 严格校验
- [x] 归档 openspec 变更（sync delta → archive）

## Validation

- `front/` 下 `pnpm check:type` 通过。
- `openspec validate fix-email-binding-flow --strict` 通过。
- 浏览器加载实际页面组件及 API 客户端，连接独立本地模拟接口：绑定及找回的发码请求分别耗时约 11.1 秒，均成功返回；未向真实邮箱发信或修改真实账号。
- 验证初始验证码输入和绑定按钮可见、空邮箱/密码校验、发送中禁止重复操作、发送失败保留所有输入、键盘回车发送、60 秒冷却及原位重发。
- 验证修改邮箱清空旧验证码并禁用绑定；验证码格式不合法或模拟服务拒绝时无法绑定；正确验证码绑定成功、状态刷新并清空表单。
- 通过实际语言按钮切换检查中英文；在约 310px 窗口验证验证码输入与右侧按钮不挤压消失。
