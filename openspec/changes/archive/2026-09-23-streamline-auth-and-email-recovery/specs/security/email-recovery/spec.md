## Purpose

允许用户验证并绑定安全邮箱，通过图形验证码和邮箱验证码找回密码，防止仅凭账号名称或未验证联系邮箱接管账号。

## ADDED Requirements

### Requirement: 验证后绑定安全邮箱

已登录用户 MUST 通过当前密码和发送到目标邮箱的一次性验证码绑定本人安全邮箱。绑定信息 MUST 独立于普通联系邮箱存储；修改联系邮箱或管理员编辑用户资料 MUST NOT 改变已验证安全邮箱。发送与确认 MUST 验证当前密码，确认时 MUST 校验挑战所属用户、邮箱、用途及令牌版本。

#### Scenario: 初次绑定或更换邮箱

- **WHEN** 用户提交正确当前密码和目标邮箱收到的有效验证码
- **THEN** 保存该用户的已验证安全邮箱，更新绑定时间并返回成功

#### Scenario: 伪造或失效绑定凭据

- **WHEN** 密码不正确、验证码错误/过期/重放、挑战属于其他用户或凭据版本已改变
- **THEN** 拒绝绑定，原绑定保持不变

### Requirement: 通过邮箱找回密码

系统 MUST 提供账号加图形验证码发起找回的接口，仅向启用账号已验证的安全邮箱发码。不存在、停用、未绑定账号 MUST 返回相同的发送提示和不可用随机挑战标识，MUST NOT 泄露邮箱地址。确认重置 MUST 要求正确邮箱验证码、新密码及确认密码；密码 MUST 为 8–64 位 ASCII 可见字符且包含字母和数字。系统 MUST 再次检查账号启用状态、当前绑定邮箱和令牌版本，事务保存 BCrypt 密码并提升令牌版本使旧 access/refresh token 立即失效。

#### Scenario: 完成找回

- **WHEN** 用户通过图形校验并提交已绑定邮箱收到的验证码及合法且一致的新密码
- **THEN** 密码更新，旧凭证失效，页面引导返回登录，不自动登录

#### Scenario: 信息不足或验证码无效

- **WHEN** 账号未绑定安全邮箱，或验证码错误、过期、被重放，或绑定/凭据已变化
- **THEN** 不允许重置，提示验证码无效或联系管理员，不提供仅凭图形验证码的重置路径

### Requirement: 邮箱验证码与发送防护

邮件验证码 MUST 由安全随机数生成，为 6 位数字、5 分钟有效、最多 5 次校验尝试，成功 MUST 通过 Redis 原子校验销毁。不同用途 MUST 隔离，验证码 MUST NOT 在 API 响应或日志中明文回显。每账号同用途发信 MUST 间隔至少 60 秒，接口 MUST 限流并防重复提交；Redis 不可用 MUST 拒绝操作。

#### Scenario: 重复请求或暴力猜测

- **WHEN** 请求命中发送冷却、接口限流或验证码尝试上限
- **THEN** 不继续发信或重置密码，验证码在尝试用尽后失效

#### Scenario: SMTP 未配置或发送失败

- **WHEN** 邮件功能未配置或 SMTP 发送失败
- **THEN** 返回明确可本地化的不可用错误，页面提供联系管理员与返回登录入口，不声称发送成功

### Requirement: 精简认证界面

登录页 MUST 仅提供账号密码登录、记住账号与忘记密码，不显示注册、手机号、扫码或第三方登录入口。认证布局 MUST 使用贵源项目文案替代通用前端模板介绍；相关页面 MUST 中英双语并在路由切换后正确渲染。

#### Scenario: 打开认证页面

- **WHEN** 用户打开登录或忘记密码页面
- **THEN** 显示项目品牌文案，不出现已移除的登录方式
