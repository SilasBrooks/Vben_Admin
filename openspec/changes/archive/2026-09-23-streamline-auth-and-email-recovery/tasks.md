## 1. 实现

- [x] 1.1 移除创建账号入口、注册路由、页面、API、状态和专用文案
- [x] 1.2 移除后端公开注册实现、白名单、注册消息和测试
- [x] 1.3 更新 README，保留登录状态初始化修复与管理员账号管理
- [x] 1.4 移除手机号、扫码、第三方登录及占位路由，替换项目认证文案
- [x] 1.5 实现 SMTP 邮件发送、Redis 一次性验证码及限流、专用绑定表和存量 SQL
- [x] 1.6 实现邮箱绑定和密码找回 API、令牌失效及核心安全测试
- [x] 1.7 实现个人中心邮箱绑定、忘记密码流程、中英双语与未配置提示

## 2. 验证

- [x] 2.1 前端类型检查、后端 clean compile 与测试通过
- [x] 2.2 重启本项目后端，检查注册接口不再发布且匿名访问被拒绝、登录验证码正常
- [x] 2.3 浏览器确认登录无创建账号入口，旧注册地址不能显示注册表单
- [x] 2.4 严格校验 openspec 变更
- [x] 2.5 归档 openspec 变更（sync delta → archive）

## 验证记录

- Java 21 干净编译通过；启用 `RECOVERY_INTEGRATION_TEST=true` 后 `mvn test` 95 项全部通过，无失败或跳过。
- 真实 HTTP/PostgreSQL/Redis 验证邮箱绑定、联系邮箱修改不影响安全邮箱、重置密码、新密码登录及旧令牌 401；邮件投递器采用测试实现，不向外部发送邮件。临时账号、绑定和登录日志已清理，用户数恢复为 4。
- Redis 集成测试验证验证码按用途隔离、5 次错误失效、原子单次消费、并发仅一个成功及过期拒绝；单测覆盖 SMTP 未配置、发送失败、账号/绑定/版本变更和密码规则。
- `pnpm check:type` 通过；浏览器确认项目文案、仅账号密码登录、忘记密码未配置提示、返回登录和旧注册链接回退。未在浏览器代填验证码或变更真实用户密码。
- 已启动更新后的本项目 18080 后端（前端 5777 代理）；OpenAPI 无注册接口，匿名注册返回 401，登录图形验证码正常，邮件功能当前为未配置状态。
- `openspec validate streamline-auth-and-email-recovery --strict` 通过；`git diff --check` 无空白错误。
