## 1. 实现

- [x] 1.1 实现动态路由准备、替换、组件复用和当前页面回退
- [x] 1.2 菜单管理改为应用内同步并提供失败重试，更新弹窗菜单选项
- [x] 1.3 接入响应式按钮权限并同步 README 与中英文提示

## 2. 验证

- [x] 2.1 验证路由新增删除、组件状态保留、失效页面回退、同步失败与会话隔离、按钮权限动态变化
- [x] 2.2 运行前端类型检查和相关测试，完成可用环境的浏览器验证
- [x] 2.3 openspec validate refresh-menu-without-page-reload --strict
- [x] 2.4 归档 openspec 变更（sync delta → archive）

## 验证记录

- `pnpm check:type` 通过。
- Vitest 路由守卫、菜单同步、响应式权限指令共 3 个测试文件、22 项测试通过。使用真实 Vue Router 和挂载的 Vue 页面，覆盖输入和组件实例保留、URL 参数和锚点保留、重复刷新不残留旧路由、组件变化、新增菜单导航、失效标签清理、首页回退、读取失败重试、串行更新、账号切换、正常 token 续签与同账号退出重登、按钮权限撤销与恢复。
- 在 `http://localhost:5777` 使用独立临时账号、角色和菜单完成浏览器验证：编辑标题后列表与侧边栏同步更新，当前查询参数和锚点保留，编辑前后无新增 Vite 文档连接日志；修改临时菜单组件后可进入新页面；停用当前临时菜单后回到仍可访问的 `/system/menu`，侧边栏移除停用项。
- 浏览器测试会话已清空并关闭；临时测试数据及一次性登录页面在验证后清理。
- `openspec validate refresh-menu-without-page-reload --strict` 通过。
